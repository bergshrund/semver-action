const { getLastVersion } = require('../src/semver')
const { expect } = require('@jest/globals')

describe('semver.js', () => {
  it('returns correct semver', async () => {
    expect('').toMatch('')
  })
})
